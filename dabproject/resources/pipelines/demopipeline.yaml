resources:
  pipelines:
    pipeline_demo_dab_pipeline:
      name: demo_dab_pipeline
      libraries:
        - glob:
            include: ../../src/pipelines/demo_dab_pipeline/transformations/**
      schema: bronze
      continuous: false
      development: false
      photon: true
      channel: CURRENT
      catalog: ${var.catalog_name}
      serverless: true
      root_path: ../../src/pipelines/demo_dab_pipeline
